# amazon-annotations 開発仕様書

## 1. 概要

`amazon-annotations` は、Amazon.co.jpの商品ページ上で、商品ごとのNG登録とコメント管理を行うための自分専用Chrome拡張機能である。

主な目的は、Amazonの商品詳細ページや商品一覧ページを閲覧しているときに、「過去に避けると決めた商品」「注意点をメモした商品」「あとで判断したい商品」を、ページ上で即座に判別できるようにすること。

公開配布は前提としない。Chrome Web Store公開、審査対応、多人数利用、外部サーバー連携、課金機能などは対象外とする。

### 1.1 本仕様で作成するMVP

この仕様書を別の生成系AIに渡した場合、まず以下のMVPを再現することを最優先とする。

* Manifest V3のChrome拡張として、ビルドツールなしのHTML/CSS/JavaScriptで構成する
* 商品詳細ページでは、popupからNG、コメント、保護フラグを保存できる
* 商品詳細ページ上では、保存済み商品のNG、コメント、保護をタイトル付近の注釈枠に表示する
* 検索結果ページでは、保存済みでNG、コメント、保護のいずれかがあるASINだけを照合対象にする
* 検索結果ページの商品検出は、まず「カートに入れる」導線のform/button周辺を起点にASINと商品カードを特定する
* カート導線で見つからない場合のみ、保存済みASINに限定して商品リンクやASIN属性を確認する
* 一覧では、NG商品をグレーアウト、コメントあり商品を黄色枠、保護済み商品を青緑枠で表示する
* コメント本文は、`メモあり` バッジへのマウスオーバーまたはfocus時に表示する
* options pageでは、保存件数、NG件数、コメント件数、保護件数、使用容量、検索、絞り込み、JSONエクスポート/インポート、手動削除を提供する

このMVPと異なる仕様へ拡張する場合は、まずこのMVPを動作する状態にしてから行う。

---

## 2. 開発方針

### 2.1 最優先すること

* 自分専用ツールとして、実用上壊れにくいこと
* Amazon.co.jpの商品詳細ページで確実に動くこと
* Amazon.co.jpの検索結果・一覧ページで、可能な範囲でNG/コメント情報を表示できること
* データはローカルに保存し、外部送信しないこと
* データが短期間で勝手に削除されないこと
* まずMVPを完成させ、その後に対象ページや機能を広げられる構成にすること

### 2.2 優先しないこと

* Chrome Web Storeでの公開
* 複数ユーザー対応
* 外部サーバー保存
* Amazon以外のECサイト対応
* 価格履歴の自動収集
* レビュー収集
* レコメンド分析
* UIの過度な作り込み

---

## 3. 対象環境

* ブラウザ: Google Chrome
* 拡張機能仕様: Manifest V3
* 対象サイト: Amazon.co.jp
* 対象URL例:

  * `https://www.amazon.co.jp/dp/...`
  * `https://www.amazon.co.jp/gp/product/...`
  * `https://www.amazon.co.jp/s?...`
  * `https://www.amazon.co.jp/*/s?...`

---

## 4. 用語

### ASIN

Amazonの商品識別子。基本的に商品管理のキーとして使用する。

### NG

ユーザーが「今後この商品を避けたい」「一覧で目立つようにしたい」と判断した状態。

### コメント

商品に対する自由記述メモ。購入候補、レビュー注意、サイズ注意、素材注意、過去に見た感想などを記録する。

### 注釈 / Annotation

NG状態、コメント、保護、アーカイブなど、商品に紐づけて保存するユーザー独自情報の総称。

---

## 5. 機能要件

## 5.1 商品詳細ページ対応

Amazon.co.jpの商品詳細ページで以下を実現する。

### 必須

* 現在表示中の商品ASINを取得する
* 拡張機能アイコンのpopupから、現在の商品をNG登録できる
* 既にNG登録済みの場合、popupからNG解除できる
* popupから商品コメントを保存できる
* 保存済みコメントをpopupで編集できる
* 商品詳細ページ上にNG状態を表示する
* 商品詳細ページ上にコメントを表示する
* 商品詳細ページ上に保護状態を表示する

### 表示位置

初期実装では、商品タイトル付近に表示エリアを差し込む。

候補:

* 商品タイトル直下
* 商品タイトルの上
* 価格情報の近く

Amazon側のDOM変更に備え、表示位置が見つからない場合でもエラーで止めず、別の安全な場所に表示する。

### 詳細ページ上の表示内容

NG設定済みの場合:

* `NG設定済み` のようなバッジ
* ページ上部またはタイトル付近への明示表示
* 可能であれば商品画像または主要領域の薄いグレーアウト

コメントがある場合:

* コメント本文を表示
* 長文の場合は折りたたみ、または最大行数を制限する
* popupで編集できる導線を持つ

保護設定済みの場合:

* `保護` バッジを表示する
* 保護状態は自動削除や整理対象から除外する判断に使う

---

## 5.2 商品一覧ページ対応

Amazon.co.jpの検索結果ページ・一覧ページで、商品カードごとに保存済みデータを照合し、NG、コメント、保護状態を表示する。

### MVP対象

まずはAmazon検索結果ページを対象とする。

例:

* `https://www.amazon.co.jp/s?k=...`

### 必須

* ページ内の商品カードからASINを抽出する
* 保存済みASINと照合する
* NG商品は一覧上で目立つようにする
* コメントあり商品は「コメントあり」または「メモあり」と表示する
* コメントあり商品にマウスオーバーしたとき、コメント内容を確認できるようにする
* 保護済み商品は「保護」と表示し、一覧上で判別できるようにする

### 現行MVPの一覧検出方針

一覧ページはDOM構造の差異が大きく、全商品カードや全商品リンクを広く走査すると負荷が高くなる。

そのため、現行MVPでは以下の方針とする。

* 一覧ページでは、保存済みでNG、コメント、保護のいずれかがあるASINだけを照合対象にする
* まず「カートに入れる」導線のform/button/input周辺からASINを取得する
* カート導線周辺から、親の商品カードDOMへさかのぼる
* カート導線で見つからない場合のみ、保存済みASINに限定して `data-asin`、`data-csa-c-asin`、`/dp/{ASIN}`、`/gp/product/{ASIN}` を確認する
* MutationObserverを使う場合、ページ全体を毎回再走査せず、追加されたDOM範囲をdebounceして部分走査する
* 一覧ページでは、`lastViewedAt` の大量更新は行わない

この方針により、売り切れ、バリエーション選択必須、広告枠などで「カートに入れる」導線や安定したASIN属性がない商品は、表示対象外になる場合がある。

### NG商品の表示

現行MVPでは以下を行う。

* 商品カード全体を薄くグレーアウト
* `NG` バッジを表示
* 商品リンクやカート操作を妨げるオーバーレイは使わない

注意:

* 初期実装では商品リンクのクリック無効化は行わない
* クリック無効化は将来オプション扱いとする

### コメントあり商品の表示

* 商品カード上に `メモあり` または `コメントあり` バッジを表示
* 商品カードを黄色系の枠で囲む
* マウスオーバーでコメントを表示
* 長文の場合は省略表示する
* バッジは商品名の長さに影響されにくいよう、商品カード先頭側に表示する

### 保護済み商品の表示

* 商品カード上に `保護` バッジを表示
* 商品カードを青緑系の枠で囲む
* 保護済み商品は整理候補や将来の自動削除対象から除外する

---

## 5.3 popup機能

Chrome右上の拡張機能アイコンから開くpopupを用意する。

### 商品詳細ページを開いている場合

表示する情報:

* 商品タイトル
* ASIN
* 現在のNG状態
* コメント入力欄
* 保存ボタン
* NG登録/解除ボタン
* コメント削除ボタン
* 保護フラグ

操作:

* NG登録
* NG解除
* コメント保存
* コメント削除
* 保護ON/OFF

### Amazon商品ページではない場合

以下のような表示にする。

* `Amazon.co.jpの商品詳細ページで利用できます`
* 設定画面へのリンク
* データ管理画面へのリンク

---

## 5.4 設定・データ管理画面

options pageを用意する。

### 必須

* 保存件数を表示
* NG件数を表示
* コメントあり件数を表示
* 保護件数を表示
* 使用容量を表示
* JSONエクスポート
* JSONインポート
* 古いデータの整理候補表示
* 選択したデータの手動削除
* 整理候補の一括削除

### あるとよい

* ASIN検索
* コメント全文検索
* NG商品のみ表示
* コメントあり商品のみ表示
* 保護済み商品のみ表示
* 最終表示日時順
* 最終編集日時順
* 全データ削除

---

## 6. 非機能要件

### 6.1 データ保存

* データはローカル保存を基本とする
* 外部サーバーには送信しない
* Googleアカウント同期は初期実装では行わない
* `chrome.storage.local` を使用する
* 必要であれば `unlimitedStorage` 権限を使用してよい
* 将来の移行に備え、データ構造はバージョン番号を持つ

### 6.2 プライバシー

* 閲覧中のAmazon商品情報を外部送信しない
* コメント内容を外部送信しない
* アクセス解析やテレメトリは実装しない
* 自分専用ツールなので、認証やユーザー管理は不要

### 6.3 保守性

AmazonのDOM構造は変わりやすいため、以下を守る。

* DOMセレクタを一箇所に集約する
* ASIN抽出処理を関数化する
* 商品カード検出処理を関数化する
* 表示差し込み処理を関数化する
* 処理失敗時にページ全体を壊さない
* `console.warn` などで失敗理由を確認しやすくする

### 6.4 パフォーマンス

一覧ページでは大量の商品カードが存在する可能性がある。

* 同じ商品カードを何度も処理しない
* 処理済みマーカーを付ける
* MutationObserverを使う場合、過剰に再処理しない
* storage読み込みはできるだけまとめる
* DOM走査は範囲を絞る

---

## 7. データ設計

## 7.1 基本方針

ASINを主キーとして、商品ごとの注釈データを保存する。

### データ例

* ASIN
* 商品タイトル
* 商品URL
* NGフラグ
* コメント
* 保護フラグ
* アーカイブフラグ
* 作成日時
* 更新日時
* 最終表示日時
* 最終編集日時
* データスキーマバージョン

---

## 7.2 商品注釈データ

1商品ごとの保存項目は以下とする。

| 項目            | 型       | 必須 | 説明                |
| ------------- | ------- | -: | ----------------- |
| asin          | string  | 必須 | Amazonの商品識別子      |
| title         | string  | 任意 | 取得できた商品タイトル       |
| url           | string  | 任意 | 商品詳細ページURL        |
| isNg          | boolean | 必須 | NG状態              |
| comment       | string  | 任意 | 自由記述コメント          |
| protected     | boolean | 必須 | 自動削除・一括削除から保護するか  |
| archived      | boolean | 必須 | アーカイブ状態           |
| createdAt     | string  | 必須 | 作成日時、ISO 8601     |
| updatedAt     | string  | 必須 | 更新日時、ISO 8601     |
| lastViewedAt  | string  | 任意 | 最後にページ上で検出・表示した日時 |
| lastEditedAt  | string  | 任意 | 最後にユーザーが編集した日時    |
| schemaVersion | number  | 必須 | データ構造バージョン        |

---

## 7.3 storage key設計

候補:

* 設定: `AmazonAnnotations:settings`
* 商品注釈データ: `AmazonAnnotations:items`
* メタ情報: `AmazonAnnotations:meta`

MVPでは、商品データを1つの大きなオブジェクトとして保存してよい。

ただし、将来的に件数が増えた場合に備えて、ASIN単位の分割保存に移行できるようにする。

### MVP案

* `items` オブジェクトの中に ASIN をキーとして商品データを保存する

### 将来案

* `item:{ASIN}` のように商品ごとに分割保存する
* インデックス情報を別途持つ

---

## 8. 容量管理仕様

## 8.1 基本方針

この拡張機能のデータは、ユーザーの個人メモであり、勝手に短期間で削除されるべきではない。

そのため、初期実装では自動削除を行わない。

容量不足対策は、以下の順で対応する。

1. 容量使用量を表示する
2. 一定以上で警告する
3. 手動整理機能を提供する
4. JSONエクスポートでバックアップできるようにする
5. 自動削除は明示的にONにした場合のみ動作する

---

## 8.2 容量表示

options pageに以下を表示する。

* 保存件数
* 使用容量
* NG件数
* コメントあり件数
* 保護件数
* 最終バックアップ日時は将来拡張扱いでよい

---

## 8.3 容量警告

使用容量に応じて警告を表示する。

### 目安

* 70%以上: 軽い注意
* 85%以上: 整理・バックアップを推奨
* 95%以上: 保存時に強めの警告

警告しても、ユーザー操作なしに削除はしない。

---

## 8.4 手動整理

options pageに整理候補を表示する。

整理候補例:

* NG解除済み、コメントなしの商品
* コメントなし、かつ長期間表示していない商品
* アーカイブ済みの商品
* 最終表示から2年以上経過した商品
* 保護されていない商品

ユーザーが選択して削除する。

---

## 8.5 自動削除仕様

自動削除は初期状態OFF。

ONにした場合のみ、容量逼迫時に動作する。

### 自動削除を実行してよい条件

* 使用容量が95%以上
* 自動削除設定がON
* 削除対象が保護されていない
* 削除対象がコメントなし
* 削除対象がNG中ではない
* 削除対象が最近表示されていない

### 自動削除しないもの

* コメントあり商品
* NG中の商品
* 保護済み商品
* 最終表示から180日未満の商品
* 最終編集から2年未満の商品

### 削除優先順位

1. 一時データ・キャッシュ
2. NG解除済み、コメントなしの商品
3. アーカイブ済み、コメントなしの商品
4. 長期間表示していない、コメントなしの商品
5. それ以外は原則削除しない

### 自動削除前の安全策

* 可能なら削除前に自動バックアップデータを作る
* 削除件数をoptions pageに履歴表示する
* 自動削除されたASIN一覧を確認できるようにする

---

## 8.6 エクスポート/インポート

### エクスポート

* 全データをJSONでダウンロードできるようにする
* ファイル名に日付を含める
* settings、items、metaを含める

ファイル名例:

`amazon-annotations-backup-2026-05-10.json`

### インポート

* JSONファイルから復元できるようにする
* 既存データとマージするか、上書きするかを選べるようにする
* 同じASINが存在する場合は、更新日時が新しいものを優先する
* インポート前に現在のデータをエクスポートする導線を用意する

---

## 9. ASIN抽出仕様

## 9.1 詳細ページ

以下の順でASINを取得する。

1. URLの `/dp/{ASIN}`
2. URLの `/gp/product/{ASIN}`
3. canonical URL
4. ページ内のASINらしきhidden inputやdata属性
5. その他Amazonページ内で安定して取得できる情報

ASINは英数字10文字を基本とする。

## 9.2 一覧ページ

一覧ページでは、「カートに入れる」導線を優先してASINを取得する。

優先順:

1. 「カートに入れる」導線周辺の `data-asin`
2. 「カートに入れる」導線周辺の `data-csa-c-asin`
3. add-to-cart form内の `input[name="asin"]`、`input[name="ASIN"]`、`input[name="asin.0"]`、`input[name="ASIN.0"]`
4. add-to-cart formのaction URL内の `asin`、`ASIN`、`asin.0`、`ASIN.0`
5. 保存済みASINに限定した、商品カードや商品リンクの `data-asin`
6. 保存済みASINに限定した、商品カードや商品リンクの `data-csa-c-asin`
7. 保存済みASINに限定した、商品リンクURL内の `/dp/{ASIN}`
8. 保存済みASINに限定した、商品リンクURL内の `/gp/product/{ASIN}`

注意:

* スポンサー商品、関連商品、バリエーションなどでDOM構造が異なる可能性がある
* 取得できない商品カードは無視してよい
* 取得失敗でページ表示を壊さない
* 一覧ページで全商品リンクを無制限に走査しない
* 売り切れ、在庫なし、バリエーション選択必須などで「カートに入れる」導線がない商品は、現行MVPでは表示対象外になる場合がある

---

## 10. 画面表示仕様

## 10.1 共通デザイン方針

* Amazon本来のUIを壊さない
* 小さくても視認できる表示にする
* NGは明確に分かるようにする
* コメントありは控えめに分かるようにする
* 自分専用なので過度にデザインしすぎない

---

## 10.2 詳細ページ表示

### NGあり

* `NG設定済み` バッジを表示
* 可能であれば枠線または背景色で注意表示
* 商品購入ボタンを勝手に無効化しない

### コメントあり

* 商品タイトル付近にコメント枠を表示
* 長文の場合は省略表示する
* popupで編集可能にする

---

## 10.3 一覧ページ表示

### NGあり

* 商品カード全体を薄くグレーアウト
* `NG` バッジを表示
* 現行MVPでは、商品リンクやカート操作を妨げるオーバーレイは使わない

### コメントあり

* 商品カードを黄色系の枠で囲む
* 商品カード先頭側に `メモあり` バッジを表示
* hoverまたはfocusでコメントを表示
* コメントが長い場合は最大文字数で省略
* 商品名が長い場合でも押し出されにくいよう、商品名の後ろではなくカード先頭側に差し込む

### 保護あり

* 商品カードを青緑系の枠で囲む
* 商品カード先頭側に `保護` バッジを表示
* コメントありと保護が両方ある場合は、黄色枠と青緑枠を重ねて表示する

---

## 11. 拡張機能構成

推奨構成:

```text
amazon-annotations/
  manifest.json
  src/
    content/
      amazon-content.js
      amazon-content.css
    popup/
      popup.html
      popup.js
      popup.css
    options/
      options.html
      options.js
      options.css
    lib/
      asin.js
      storage.js
      amazon-dom.js
      constants.js
  README.md
  specification.md
```

ビルドツールは必須ではない。

まずは素のHTML/CSS/JavaScriptで実装してよい。

現行MVPでは、`background/service-worker.js` と `icons/` は未作成である。必要になった時点で追加する。

---

## 12. manifest方針

Manifest V3を使用する。

### 必要な権限

* `storage`
* `activeTab`
* `scripting`
* `unlimitedStorage`
* Amazon.co.jpへのhost permission

### host permissions

対象はAmazon.co.jpに限定する。

例:

* `https://www.amazon.co.jp/*`
* 必要なら `https://*.amazon.co.jp/*`

---

## 13. 実装ステップ

## Phase 1: 詳細ページMVP

目的:

商品詳細ページで、NG登録・解除・コメント保存・ページ上表示ができること。

作業:

1. Manifest V3の最小構成を作る
2. Amazon.co.jpの商品詳細ページでcontent scriptを動かす
3. ASIN抽出関数を実装する
4. popupで現在ページのASINを表示する
5. `chrome.storage.local` に商品データを保存する
6. NG登録・解除を実装する
7. コメント保存・編集を実装する
8. 詳細ページ上にNG/コメント表示を差し込む

完了条件:

* 商品詳細ページでASINが取得できる
* popupからNG登録できる
* popupからNG解除できる
* popupからコメント保存できる
* ページ再読み込み後も表示が残る

---

## Phase 2: 検索結果ページ対応

目的:

Amazon検索結果ページで、NG商品、コメントあり商品、保護済み商品を識別できること。

作業:

1. 検索結果ページで「カートに入れる」導線を検出する
2. カート導線周辺からASINを取得する
3. カート導線周辺から親の商品カードDOMを特定する
4. カート導線で取れない場合は、保存済みASINに限定して商品リンクやASIN属性を確認する
5. 保存済みデータと照合する
6. NG商品をグレーアウトする
7. コメントあり商品を黄色枠で囲み、`メモあり` バッジを表示する
8. 保護済み商品を青緑枠で囲み、`保護` バッジを表示する
9. hover/focusでコメントを表示する
10. MutationObserverで後から追加された商品にも対応する
11. MutationObserverではページ全体の過剰な再走査を避ける

完了条件:

* 検索結果ページでNG商品がグレーアウトされる
* コメントあり商品に黄色枠と `メモあり` バッジが出る
* 保護済み商品に青緑枠と `保護` バッジが出る
* hoverでコメントを確認できる
* ページスクロールや追加読み込み後も可能な範囲で反映される

---

## Phase 3: options page / データ管理

目的:

保存データの確認、バックアップ、整理ができること。

作業:

1. options pageを作る
2. 保存件数、NG件数、コメント件数、保護件数を表示する
3. 使用容量を表示する
4. JSONエクスポートを実装する
5. JSONインポートを実装する
6. 手動削除を実装する
7. 整理候補表示を実装する

完了条件:

* 保存件数が確認できる
* 使用容量が確認できる
* バックアップJSONを出力できる
* JSONから復元できる
* 不要データを手動削除できる

---

## Phase 4: 容量管理強化

目的:

長期利用に耐える容量管理を行う。

作業:

1. 容量警告を実装する
2. 70%、85%、95%の警告表示を実装する
3. 自動削除設定を追加する
4. 自動削除対象判定を実装する
5. 削除履歴を表示する

完了条件:

* 容量逼迫時に警告が出る
* 自動削除は初期OFFである
* ONにした場合でも、コメントあり・NG中・保護済み・最近表示した商品は削除されない

---

## 14. 実装時の注意点

### 14.1 AmazonのDOM変更への対策

Amazonのページ構造は変わることがある。

そのため、セレクタを直接あちこちに書かない。

必ず以下のように分離する。

* ASIN抽出
* 商品カード検出
* 詳細ページの表示差し込み位置検出
* 一覧ページの表示差し込み位置検出

### 14.2 二重挿入防止

content scriptは再実行される可能性がある。

* 差し込み済み要素には専用classまたはdata属性を付ける
* 既に差し込み済みなら再挿入しない
* 商品カードにも処理済みマーカーを付ける

### 14.3 エラー処理

Amazonページ上で拡張機能がエラーを出しても、ページ閲覧を妨げない。

* try/catchを適切に使う
* 取得失敗時は静かに無視する
* デバッグしやすい警告ログは残す

### 14.4 保存タイミング

* popupの保存ボタンで明示保存する
* NG登録/解除は即時保存でよい
* 保護ON/OFFは即時保存でよい
* コメントは保存ボタンで明示保存する
* `lastViewedAt` は既存データがある商品の詳細ページ閲覧時に更新する
* 商品詳細ページを開いただけでは、空の注釈データを新規作成しない
* 一覧ページでは大量書き込みを避けるため、`lastViewedAt` は更新しない

---

## 15. 受け入れ条件

## 15.1 商品詳細ページ

* Amazon.co.jpの商品詳細ページでpopupを開くとASINが表示される
* NG登録できる
* NG解除できる
* コメントを保存できる
* コメントを再編集できる
* 保護ON/OFFできる
* ページ上にNG表示が出る
* ページ上にコメント表示が出る
* ページ上に保護表示が出る

## 15.2 検索結果ページ

* NG登録済みの商品が検索結果に出た場合、グレーアウトされる
* コメントありの商品には黄色枠と `メモあり` バッジが出る
* 保護済みの商品には青緑枠と `保護` バッジが出る
* バッジのhoverでコメントが表示される
* 未登録商品には余計な表示が出ない
* 商品リンクやカート操作を妨げない

## 15.3 データ管理

* options pageで保存件数が見える
* options pageで使用容量が見える
* JSONエクスポートできる
* JSONインポートできる
* 手動削除できる
* ASIN、タイトル、コメントで検索できる
* NG、コメントあり、保護済み、整理候補で絞り込める

## 15.4 安全性

* コメントあり商品は勝手に削除されない
* NG中の商品は勝手に削除されない
* 保護済み商品は勝手に削除されない
* 外部通信を行わない

---

## 16. 将来拡張候補

MVP後に検討する。

* タグ機能

  * `サイズ注意`
  * `レビュー注意`
  * `素材注意`
  * `購入候補`
  * `保留`
  * `購入済み`
* 商品カードから直接NG登録
* 商品カードから直接コメント編集
* 商品詳細ページに固定ミニパネルを表示
* 親ASIN・バリエーション単位のNG
* 楽天市場やYahoo!ショッピング対応
* IndexedDBへの移行
* コメント履歴
* 価格メモ
* 購入済み管理
* データ検索画面の強化

---

## 17. 開発する生成系AIへの作業指示

この仕様書を読んだうえで、以下の順序で開発すること。

1. まず仕様を読み、MVP範囲を確認する
2. 不明点があっても、MVPに不要なものは後回しにする
3. まずPhase 1の商品詳細ページ対応を完成させる
4. その後、Phase 2の検索結果ページ対応に進む
5. DOMセレクタやASIN抽出処理は保守しやすいように分離する
6. Chrome拡張はManifest V3で作る
7. 外部サーバー通信は実装しない
8. データは `chrome.storage.local` に保存する
9. コードは省略せず、動作するファイル一式として出力する
10. READMEにインストール方法、使い方、開発者モードでの読み込み方法を書く
11. 実装後、手動テスト手順をREADMEに記載する

### 17.1 生成すべきファイル

最低限、以下のファイルを生成する。

```text
manifest.json
src/
  content/
    amazon-content.css
    amazon-content.js
  lib/
    amazon-dom.js
    asin.js
    constants.js
    storage.js
  options/
    options.css
    options.html
    options.js
  popup/
    popup.css
    popup.html
    popup.js
README.md
specification.md
```

現行MVPでは、service worker、アイコン画像、ビルド設定、外部ライブラリは不要である。

### 17.2 ライブラリ分割の責務

実装時は、最低限以下の責務でファイルを分ける。

* `constants.js`
  * storage key、schema version、CSS classやdata属性名などの定数
* `asin.js`
  * ASIN正規化
  * URLからのASIN抽出
  * 詳細ページdocumentからのASIN抽出
* `storage.js`
  * `chrome.storage.local` の読み書き
  * 商品データの正規化
  * upsert/delete/import/export
  * stats計算
* `amazon-dom.js`
  * Amazon詳細ページの差し込み先検出
  * 検索結果ページの商品カード検出
  * カート導線周辺からのASIN抽出
* `amazon-content.js`
  * 詳細ページ上の注釈描画
  * 検索結果ページ上の商品カード装飾
  * storage変更監視
  * MutationObserver
* `popup.js`
  * active tab情報取得
  * popupフォーム表示
  * NG/コメント/保護の保存
* `options.js`
  * stats表示
  * 検索/絞り込み
  * JSON export/import
  * 選択削除、整理候補削除

### 17.3 実装で必ず守ること

* 一覧ページでは、ページ上の全商品リンクを無制限に走査しない
* 一覧ページでは、保存済みでNG、コメント、保護のいずれかがあるASINだけを照合対象にする
* 一覧ページでは、まず「カートに入れる」導線を起点にする
* カート導線で取れない場合だけ、保存済みASINに限定して商品リンクやASIN属性を確認する
* `メモあり` バッジは商品名の後ろに差し込まない。商品名が長いと表示範囲外へ押し出されるため、商品カード先頭側にprependする
* NG、コメント、保護は同じ商品カード装飾処理で扱う
* 商品カードには処理済みASINをdata属性などで記録し、同じカードを何度も処理しない
* storage変更時は必要なページだけ再描画する。詳細ページで一覧用処理を走らせない
* 拡張機能再読み込み後に古いcontent scriptが動く場合の `Extension context invalidated` は、既知の一時エラーとして警告を抑制してよい
* popupで `chrome.scripting.executeScript` が `Frame with ID ... was removed` を出す場合があるため、タブURLからASINを取得するフォールバックを持つ
* 商品詳細ページを開いただけで空の注釈データを作らない
* 一覧ページでは `lastViewedAt` を更新しない

### 17.4 詰まりどころと対策

#### 一覧ページでフリーズする

原因になりやすいこと:

* ページ内の全ASIN属性、全商品リンク、全商品カードを広くquerySelectorAllする
* MutationObserverのたびにdocument全体を再走査する
* storage読み込みとDOM走査を商品カードごとに繰り返す

対策:

* 最初にstorageを一度だけ読み、NG/コメント/保護のある保存済みASINだけのlookupを作る
* DOM検索は保存済みASINに限定する
* MutationObserverでは追加されたnodeだけをdebounceして部分走査する
* 一覧ページでは閲覧時刻の大量書き込みをしない

#### 一覧ページで`メモあり`がDOM上にあるのに見えない

原因になりやすいこと:

* 商品名の後ろやタイトル行の中にバッジを差し込む
* 商品名が長く、Amazon側のレイアウトでバッジが表示範囲外へ押し出される

対策:

* バッジコンテナは商品カード先頭側にprependする
* バッジコンテナはflex-wrap可能にし、カード幅からはみ出しにくくする
* コメント本文はカード全体のoverlayではなく、`メモあり` バッジのhover/focusツールチップで表示する

#### 詳細ページの色々な場所に`メモあり`が表示される

原因になりやすいこと:

* 詳細ページでも一覧ページ用の商品検出処理を走らせる
* 詳細ページ内のASIN付き要素を一覧カードと誤認する

対策:

* 詳細ページでは詳細用の描画だけ行う
* 一覧ページ用描画は `isSearchResultPage()` で商品詳細ページを除外する
* storage変更時も、詳細ページでは一覧用再描画を実行しない

#### popup初期化時に `Frame with ID 0 was removed` が出る

原因になりやすいこと:

* Amazonページ側の遷移、再描画、フレーム差し替え中に `chrome.scripting.executeScript` を実行する

対策:

* executeScript失敗時はタブURLからASINを抽出する
* 既知の一時エラーはconsole.warnに出し続けない

#### 拡張機能更新後に挙動が変わらない

原因になりやすいこと:

* `chrome://extensions/` で拡張機能を再読み込みしても、開いているAmazonタブには古いcontent scriptが残る

対策:

* READMEに、拡張機能再読み込み後はAmazonタブもリロードすることを明記する
* 古いcontent script由来の `Extension context invalidated` は抑制してよい

### 17.5 完了前に実行する確認

最低限、以下を実行してから完了報告する。

```sh
node --check src/lib/constants.js
node --check src/lib/asin.js
node --check src/lib/storage.js
node --check src/lib/amazon-dom.js
node --check src/content/amazon-content.js
node --check src/popup/popup.js
node --check src/options/options.js
jq . manifest.json
```

Chrome実機で確認できる場合は、READMEの手動テスト手順も実行する。

---

## 18. READMEに書くべき内容

READMEには最低限以下を含める。

* 概要
* 機能一覧
* インストール方法

  * Chromeの拡張機能管理画面を開く
  * デベロッパーモードをONにする
  * パッケージ化されていない拡張機能を読み込む
* 使い方

  * 商品詳細ページでNG登録する
  * コメントを書く
  * 保護フラグを設定する
  * 一覧ページで確認する
* データ保存場所の説明
* エクスポート/インポート方法
* 容量管理仕様
* 手動テスト手順
* 開発時の構文確認コマンド
* 拡張機能更新後にAmazonタブもリロードする注意
* 既知の制限

  * AmazonのDOM変更で表示できなくなる可能性
  * 初期対応はAmazon.co.jp中心
  * 一覧ページは検索結果ページから対応
  * 一覧ページでは「カートに入れる」導線がある商品を主対象にする
  * カート導線がない商品は表示対象外になる場合がある

---

## 19. 既知の制限

* AmazonのDOM構造変更により、ASIN抽出や表示差し込みが失敗する可能性がある
* すべてのAmazonページ種別には対応しない
* 初期実装では検索結果ページ中心
* 一覧ページでは「カートに入れる」導線がある商品を主対象にする
* 売り切れ、在庫なし、バリエーション選択必須などでカート導線がない商品は表示対象外になる場合がある
* 一覧ページでは負荷対策のため、保存済みでNG、コメント、保護のいずれかがあるASINだけを照合する
* 商品バリエーションの完全な親子管理は行わない
* Chrome同期は行わない
* スマホ版Amazon表示は対象外
* 他ブラウザ対応は対象外

---

## 20. MVPの最終ゴール

MVPでは、以下ができれば完成とする。

1. Amazon.co.jpの商品詳細ページで、ASINを取得できる
2. 拡張機能popupからNG登録・解除ができる
3. 拡張機能popupからコメント保存・編集ができる
4. 拡張機能popupから保護ON/OFFができる
5. 商品詳細ページ上にNG、コメント、保護が表示される
6. Amazon.co.jp検索結果ページ上で、NG商品がグレーアウトされる
7. コメントあり商品が黄色枠で囲われ、`メモあり` バッジとhoverコメントを確認できる
8. 保護済み商品が青緑枠で囲われ、`保護` バッジを確認できる
9. 保存データをJSONでエクスポート/インポートできる
10. 使用容量を確認できる
11. 勝手に短期間でデータが削除されない

---

## 21. この仕様でやらないこと

以下は、初期開発では実装しない。

* Chrome Web Store公開対応
* アカウント連携
* 外部サーバー保存
* 価格履歴取得
* レビュー本文取得
* AIによる商品判定
* Amazon以外のECサイト対応
* スマホブラウザ対応
* 複数人共有
